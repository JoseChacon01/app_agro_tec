import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_agro_tec',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
